# Rahasia. Jangan pernah dihapus dari daftar ini.
.env
.env.*
!.env.example

# Overlay config milik pemilik (live.enabled, tanggal verifikasi kunci, pecahan posisi trial).
# Ditulis oleh `tradebot local-set`; tidak pernah berisi kunci, tapi tetap bukan milik repo.
/config/local.yaml

# Artefak runtime, semuanya bisa dibuat ulang atau bersifat pribadi.
# Dijangkar ke root dengan '/' di depan: pola 'data/' tanpa jangkar ikut mengabaikan
# src/tradebot/data/, dan paket itu pernah hilang dari git karenanya.
/data/
/logs/
/state/
/trades/
/STOP

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Editor dan OS
.DS_Store
.idea/
.vscode/
